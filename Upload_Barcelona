from sqlalchemy import create_engine
import pymysql
import pandas as pd

user = ""
password = ""
host = "localhost"
database = "AIRBNB"
table = "Barcelona"

engine = create_engine(f"mysql+pymysql://{user}:{password}@{host}/{database}")

df = pd.read_csv("/----E/barcelona_weekdays.csv")

df.to_sql(name=table, con=engine, if_exists="append", index=False)

print("Success!")
